import java.util.Scanner;

class Solution {
        public boolean isValid(String s) {
            Scanner scanner = new Scanner(System.in);
            String s = scanner.nextLine();             // 한 줄 전체 입력받기
            if (s != (){}[]) //아닐경우 -1로 밖으로 뺴자
            for(i=0;i<s.length;i++){
                for(j=0;j<s.length;j++){
                    if (s[i] =='('&& j>i &&s[j] ==')')
                }
            }
        }
    }return new s
